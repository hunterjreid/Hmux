{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core API access for the chrome webview only.",
  "windows": ["main"],
  "webviews": ["ui"],
  "permissions": ["core:default"]
}
